//installed yup for validation,monk for mongo db(to be replaced by mongoose),helmet(security),morgan(for logs),cors,dotenv,nodemon
const express= require('express');
const cors= require('cors');
const morgan= require('morgan');
const helmet= require('helmet');